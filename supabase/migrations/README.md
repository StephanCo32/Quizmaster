# Database migrations

Add forward-only Supabase SQL migrations here. Generate a timestamped migration
with `npx supabase migration new <name>`, validate the complete history with
`npx supabase db reset`, and never edit a migration after it reaches Production.